# Post-training handoff for the Honcho dialectic model (base: stripped Qwen3.5-9B).
# FROM points at the GGUF produced by `train_dialectic.py --stage export`; adjust the path.
# No TEMPLATE line on purpose: Ollama renders the Jinja template embedded in the GGUF, which export
# patches so the generation prompt ends with the CLOSED think block (TRAIN.md §12). Verify with
#   ollama show <name> --template | tail -8   -> the default branch must emit '<think>\n\n</think>\n\n'
FROM runs/v2-gguf-500/unsloth.Q4_K_M.gguf
PARAMETER temperature 0.1
PARAMETER num_ctx 8192
